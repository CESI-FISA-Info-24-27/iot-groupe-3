name: CD Backend - Déploiement automatique

# Déclenchement uniquement après succès du CI
on:
  workflow_run:
    workflows: ["CI EcoGuard IoT"]
    types:
      - completed

jobs:
  # ========================================
  # Job : Vérification des fichiers changés
  # ========================================
  check-changes:
    name: Check Backend Changes
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main' }}
    outputs:
      backend-changed: ${{ steps.filter.outputs.backend }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Check path filter
        uses: dorny/paths-filter@v2
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'docker-compose.yml'
              - '.github/workflows/cd-backend.yml'

  # ========================================
  # Job : Déploiement sur le serveur
  # ========================================
  deploy:
    name: Deploy Backend to Server
    runs-on: ubuntu-latest
    needs: check-changes
    if: needs.check-changes.outputs.backend-changed == 'true'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Préparer le dossier de déploiement
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            # Créer le dossier de déploiement s'il n'existe pas
            mkdir -p ~/ecoguard
            cd ~/ecoguard
            
            # Créer le dossier pour la base de données SQLite (partagé avec sqlite-web)
            mkdir -p ~/data
            
            # Nettoyer les anciens fichiers backend (sauf la BDD)
            rm -rf backend
      
      - name: Copier les fichiers sur le serveur
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          source: "backend/*,docker-compose.yml"
          target: "~/ecoguard"
          overwrite: true
      
      - name: Créer le fichier .env pour le backend
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ~/ecoguard/backend
            
            # Créer le fichier .env avec la configuration SQLite
            cat > .env << EOF
            DATABASE_URL=sqlite:////app/data/ecoguard.db
            EOF
            
            echo "Fichier .env créé avec DATABASE_URL configurée"
      
      - name: Build et déployer sur le serveur
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ~/ecoguard
            
            # Créer le réseau Docker s'il n'existe pas
            docker network create ecoguard-network || true
            
            # Arrêter et supprimer l'ancien conteneur
            docker stop ecoguard-backend 2>/dev/null || true
            docker rm ecoguard-backend 2>/dev/null || true
            
            # Builder la nouvelle image
            docker build -t ecoguard-backend:latest ./backend
            
            # Démarrer le nouveau conteneur avec volume pour SQLite (partagé avec sqlite-web)
            docker run -d \
              --name ecoguard-backend \
              --restart unless-stopped \
              --security-opt apparmor=unconfined \
              -p 8081:8000 \
              --network ecoguard-network \
              -v ~/data:/app/data \
              --env-file ~/ecoguard/backend/.env \
              ecoguard-backend:latest
            
            # Attendre que le conteneur démarre
            echo "⏳ Attente du démarrage du conteneur..."
            sleep 3
            
            # Vérifier si le conteneur tourne
            if docker ps | grep -q ecoguard-backend; then
              echo "✅ Conteneur démarré avec succès"
            else
              echo "❌ Le conteneur n'a pas démarré, vérification des logs..."
              docker logs ecoguard-backend || echo "Pas de logs disponibles"
              exit 1
            fi
            
            # Nettoyer les images non utilisées
            docker image prune -f
      
      - name: Vérifier le déploiement
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ~/ecoguard
            
            # Vérifier que le conteneur tourne
            echo "🔍 Vérification du statut du conteneur..."
            docker ps -a | grep ecoguard-backend
            
            # Vérifier la politique de redémarrage automatique
            echo "🔄 Vérification de la politique de redémarrage..."
            RESTART_POLICY=$(docker inspect ecoguard-backend --format='{{.HostConfig.RestartPolicy.Name}}')
            if [ "$RESTART_POLICY" = "unless-stopped" ]; then
              echo "✅ Politique de redémarrage: $RESTART_POLICY (le conteneur redémarrera automatiquement)"
            else
              echo "⚠️ Politique de redémarrage: $RESTART_POLICY (devrait être 'unless-stopped')"
            fi
            
            # Attendre l'initialisation complète
            echo "⏳ Attente de l'initialisation (10s)..."
            sleep 10
            
            # Afficher les logs récents
            echo "📋 Logs du conteneur:"
            docker logs --tail 50 ecoguard-backend
            
            # Test de santé sur l'endpoint root
            echo "🏥 Test de santé du backend..."
            if curl -f http://localhost:8081/ 2>/dev/null; then
              echo "✅ API répond correctement"
            else
              echo "⚠️ L'API ne répond pas encore, vérification approfondie..."
              docker inspect ecoguard-backend | grep -A 10 State
            fi
            
            # Vérifier que la base de données a été créée
            echo "💾 Vérification de la base de données..."
            if [ -f ~/data/ecoguard.db ]; then
              echo "✅ Base de données SQLite accessible"
              ls -lh ~/data/ecoguard.db
              echo "ℹ️  BDD partagée avec sqlite-web sur port 8080"
            else
              echo "ℹ️  Base de données pas encore créée (normal si première initialisation)"
              ls -la ~/data/ || echo "Le dossier n'existe pas"
            fi
      
      - name: Notification de succès
        if: success()
        run: echo "Déploiement du backend réussi sur ${{ secrets.SSH_HOST }}"
      
      - name: Notification d'échec
        if: failure()
        run: |
          echo "Échec du déploiement du backend"
          exit 1
